<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="150dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cv_post_item"
    android:layout_margin="20dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    app:cardCornerRadius="10dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_vertical"
        android:orientation="vertical"
        android:padding="10dp">

        <TextView
            android:id="@+id/tv_username_post"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/gray"
            android:gravity="end"
            tools:text="Username" />

        <TextView
            android:id="@+id/tv_title_post"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:textColor="@color/primary900"
            android:textSize="20sp"
            android:textStyle="bold"
            tools:text="title" />

        <TextView
            android:id="@+id/tv_content_post"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="2"
            android:textColor="@color/primary900"
            tools:text="By using getChildFragmentManager(), you retrieve the FragmentManager associated with the child Fragment within the parent Fragment. This allows you to perform fragment transactions and manage the child fragments within the parent fragment.
Make sure you call this code within the Fragment class or one of its methods, such as onCreateView(), onActivityCreated(), or onResume(), to ensure the FragmentManager is available.
Note: If you're using the support library (AndroidX), the method getChildFragmentManager() is available. If you're not using the support library, you can use getFragmentManager() instead, as it will provide the same functionality." />

    </LinearLayout>

</androidx.cardview.widget.CardView>